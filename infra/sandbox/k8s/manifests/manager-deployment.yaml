apiVersion: apps/v1
kind: Deployment
metadata:
  name: sandbox-manager
  namespace: sandboxes
  labels:
    app: sandbox-manager
spec:
  replicas: 1
  # Single replica: avoid long "old replica pending termination" during RollingUpdate
  # (Flask/Werkzeug can be slow to exit on SIGTERM). Recreate = stop old, then start new.
  strategy:
    type: Recreate
  selector:
    matchLabels:
      app: sandbox-manager
  template:
    metadata:
      labels:
        app: sandbox-manager
    spec:
      terminationGracePeriodSeconds: 15
      serviceAccountName: sandbox-manager
      containers:
      - name: manager
        image: ghcr.io/YOUR_ORG/devpilot-manager:latest
        imagePullPolicy: Always
        ports:
        - containerPort: 8090
        envFrom:
        - secretRef:
            name: manager-secrets
        resources:
          requests:
            memory: "256Mi"
            cpu: "100m"
          limits:
            memory: "512Mi"
            cpu: "500m"
        # Python + kubernetes client import can exceed 10s on cold start; without this,
        # livenessProbe kills the container before /health responds -> rollout stuck forever.
        startupProbe:
          httpGet:
            path: /health
            port: 8090
          failureThreshold: 36
          periodSeconds: 5
          timeoutSeconds: 3
        readinessProbe:
          httpGet:
            path: /health
            port: 8090
          periodSeconds: 10
          timeoutSeconds: 3
          failureThreshold: 6
        livenessProbe:
          httpGet:
            path: /health
            port: 8090
          periodSeconds: 30
          timeoutSeconds: 3
          failureThreshold: 3
---
apiVersion: v1
kind: Service
metadata:
  name: sandbox-manager
  namespace: sandboxes
  labels:
    app: sandbox-manager
spec:
  type: NodePort
  selector:
    app: sandbox-manager
  ports:
  - name: api
    port: 8090
    targetPort: 8090
    nodePort: 30090
